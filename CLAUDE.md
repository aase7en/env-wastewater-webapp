# env-wastewater-webapp — Claude Code edition

See `AGENTS.md` — same rules apply to Claude Code. Read it first every session,
especially the **Companion repo — A-Wiki** section: this repo's domain knowledge
(wastewater schema, ENV concepts) lives in a separate sibling repo, not here.

A SessionStart hook (`.claude/hooks/session-start-companion-notice.sh`) prints
a reminder of A-Wiki's location on every session start so this isn't easy to forget.
