/**
 * @deprecated P12 — FastAPI is no longer in the runtime path. Use
 * lib/supabase-queries.ts (via lib/hooks.ts) instead. This file is kept
 * only as a reference for the original typed-FastAPI contract; it is not
 * imported by any active page/hook.
 *
 * The FastAPI app/ directory still ships in the repo for local dev /
 * reference (see docs/adr/0004-supabase-first-no-fastapi-in-prod.md), but
 * the production frontend talks to Supabase directly via RLS.
 */
