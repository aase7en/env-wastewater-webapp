/** Stub — real implementation lands in P10.6.4. */
export function DailyFormPage() {
  return (
    <div className="max-w-3xl mx-auto p-8 text-aura-textMuted font-thai">
      ฟอร์มบันทึกประจำวัน — P10.6.4 (ยังไม่เสร็จ)
    </div>
  );
}
