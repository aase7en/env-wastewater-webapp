/** Stub — real implementation lands in P10.6.5. */
export function ReadingsListPage() {
  return (
    <div className="max-w-5xl mx-auto p-8 text-aura-textMuted font-thai">
      ประวัติการบันทึก — P10.6.5 (ยังไม่เสร็จ)
    </div>
  );
}
